{
  "name": "npm-count",
  "version": "0.0.1-beta.1",
  "description": "Fetch the npm/download-counts",

  "main": "lib/index.js",
  "browser": {
    "./src": false,
    "request": false
  },

  "scripts": {
    "start": "jasminetea --file index.coffee --watch",
    "test": "jasminetea --file index.coffee --cover --report --lint",

    "prepublish": "node -e \"if(process.env.TRAVIS){}else{process.exit(1)}\" && npm run compile || echo skip prepublish",
    "compile": "coffee --output lib --bare --compile src",

    "localhost": "zuul test/index.coffee --ui jasmine2 --local 59798 --open",
    "test-cloud": "zuul test/index.coffee",

    "prebuild": "$(opc bower opc-scripts.build) | exorcist $(opc bower name).js.map > $(opc bower name).js",
    "build": "$(opc bower opc-scripts.uglify)",
    "postbuild": "rm $(opc bower name).js $(opc bower name).js.map && wc -c $(opc bower main)",
    "update": "git tag v$(opc package version) && git push --tags"
  },
  "config": {
    "ghooks": {
      "pre-commit": "npm test"
    }
  },

  "dependencies": {
    "bluebird": "^2.9.30",
    "lodash": "^3.10.0",
    "moment": "^2.10.3",
    "request": "^2.58.0",
    "xhr": "^2.0.2"
  },
  "devDependencies": {
    "ghooks": "^0.3.2",
    "jasminetea": "^0.2.1",
    
    "lodash": "^3.10.0",

    "browserify": "^10.2.4",
    "coffeeify": "^1.1.0",
    "exorcist": "^0.4.0",
    "object-parser-cli": "0.0.1-alpha.1",
    "uglify-js": "^2.4.23"
  },
  "extraDependencies": {
    "zuul": "^3.0.0"
  },
  
  "keywords": [
    "npm",
    "download-counts"
  ],
  "author": "59naga <i59naga@icloud.com> (http://berabou.me)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/59naga/npm-count.git"
  },
  "bugs": {
    "url": "https://github.com/59naga/npm-count/issues"
  },
  "homepage": "https://github.com/59naga/npm-count#readme"
}
